const escapeHtml = (value) => String(value).replace(/[&<>"']/g, (char) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#039;" }[char]));
const id = new URLSearchParams(location.search).get("id");
const load = async () => { const response = await fetch(`/api/content/${encodeURIComponent(id)}`); if (!response.ok) { document.querySelector("#preview-title").textContent = "内容不存在"; return; } const item = await response.json(); const rendered = await fetch("/api/markdown", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ content: item.content, sourcePath: item.sourcePath }) }).then((result) => result.json()); document.title = `${item.title} - Firefly`; document.querySelector("#preview-title").textContent = item.title; document.querySelector("#preview-meta").textContent = `${item.type === "article" ? "文章" : "动态"} · ${item.publishedAt || "未发布"}${item.category ? ` · ${item.category}` : ""}`; document.querySelector("#preview-content").innerHTML = rendered.html; };
load();
